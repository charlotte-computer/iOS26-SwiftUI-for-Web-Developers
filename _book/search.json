[
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "iOS26 SwiftUI for Web Developers",
    "section": "",
    "text": "Important\n\n\n\nThis is *not a legitimate Oreilly publishing, I hope it is one day.\n\n\n\nPreface\nThis is a book aimed at others on a similar journey of specialisation in Swift - from the world of React and web development.\nThis book assumes the reader has some programming experience and does not explain basics\nWriting this book as I ask AI to explain concepts to me, and making a shareable resource in the process\nThe dogs on the cover are Annie and Anubis they are adopted Greyhounds, recommend this breed to any software engineer wanting dogs - they are couch potatoes and will not bother you while you code.\nThis is dedicated to my wonderful partner Alex; who showed me the world of software engineering and gave me the power to build anything I could dream of. Thankyou for believing in me, and pushing me to learn to code. You were right, it rocks.",
    "crumbs": [
      "<span class='chapter-number'>1</span>  <span class='chapter-title'>Preface</span>"
    ]
  },
  {
    "objectID": "chapters/anatomy.html",
    "href": "chapters/anatomy.html",
    "title": "Anatomy of a Basic App",
    "section": "",
    "text": "The Folder Structure\nEvery iOS project natively uses Xcode as the development environment. The folder structure relies on specific files existing in order for the application to compile correctly.\nHigh-Level Xcode Structure:",
    "crumbs": [
      "<span class='chapter-number'>2</span>  <span class='chapter-title'>Anatomy of a Basic App</span>"
    ]
  },
  {
    "objectID": "chapters/anatomy.html#the-folder-structure",
    "href": "chapters/anatomy.html#the-folder-structure",
    "title": "Anatomy of a Basic App",
    "section": "",
    "text": "App.swift — The root entry point of the app.\nContentView.swift — The first screen the user actually sees.\nAssets.xcassets — Where you store images, custom colors, and app icons.\nInfo.plist — Configuration file containing hardware permissions and app rules.\n\n\n\n\n\n\n\nNoteWhat does this look like in React?\n\n\n\n\n\nReact projects are more flexible, but generally map similarly:\n\nindex.js (Root Entry)\nApp.js (Main Router/Screen)\npublic/ or src/assets/ (Images/Icons)\npackage.json (Configuration)",
    "crumbs": [
      "<span class='chapter-number'>2</span>  <span class='chapter-title'>Anatomy of a Basic App</span>"
    ]
  },
  {
    "objectID": "chapters/anatomy.html#the-root-entry-point",
    "href": "chapters/anatomy.html#the-root-entry-point",
    "title": "Anatomy of a Basic App",
    "section": "The Root Entry Point",
    "text": "The Root Entry Point\nWhen a user taps an app icon on their iPhone, iOS looks for the @main attribute to know strictly where the app officially starts executing.\nHere is the Swift Entry Point (App.swift):\nimport SwiftUI\n\n@main\nstruct MyApp: App {\n    var body: some Scene {\n        WindowGroup {\n            ContentView() // ⬅️ Points to the first Screen\n        }\n    }\n}\n\n\n\n\n\n\nNoteReact Equivalent (index.js)\n\n\n\n\n\nimport React from 'react';\nimport ReactDOM from 'react-dom/client';\nimport App from './App';\n\nconst root = ReactDOM.createRoot(document.getElementById('root'));\nroot.render(\n  &lt;React.StrictMode&gt;\n    &lt;App /&gt; {/* ⬅️ Points to the first Screen */}\n  &lt;/React.StrictMode&gt;\n);\n\n\n\n\nWhat About main.swift?\nBefore @main was introduced in Swift 5.3 (2020), the conventional entry point was a special file named main.swift. It is the only file in a Swift project that is allowed to contain top-level executable code — meaning code written directly at the file’s root scope, outside of any struct, class, or func.\n// main.swift (legacy entry point)\nimport UIKit\n\nUIApplicationMain(\n    CommandLine.argc,\n    CommandLine.unsafeArgv,\n    nil,\n    NSStringFromClass(AppDelegate.self)\n)\nThe name main.swift is not arbitrary — the Swift compiler treats it as a reserved filename with special rules:\n\nOnly one main.swift can exist per target. Having more than one will cause a compile error.\nTop-level expressions are only legal here. Any other .swift file that tries to run code at the top level (outside a type or function) will fail to compile.\n@main replaced it. Modern SwiftUI apps use @main on a struct instead, which is cleaner and avoids manually calling UIApplicationMain. You will still encounter main.swift in older UIKit projects or Swift command-line tools.\n\n\n\n\n\n\n\nNoteNode.js Parallel\n\n\n\n\n\nmain.swift behaves much like a Node.js entry file (e.g. index.js run directly via node index.js) — it is the one file where code runs immediately at the top level without needing to be wrapped in a function or class. All other files must export or be explicitly called.",
    "crumbs": [
      "<span class='chapter-number'>2</span>  <span class='chapter-title'>Anatomy of a Basic App</span>"
    ]
  },
  {
    "objectID": "chapters/anatomy.html#how-screens-work-views",
    "href": "chapters/anatomy.html#how-screens-work-views",
    "title": "Anatomy of a Basic App",
    "section": "How Screens Work (Views)",
    "text": "How Screens Work (Views)\nIn Swift, there is no generic &lt;div&gt;. A screen is always built strictly as a Struct conforming to the View protocol.\nHere is what ContentView.swift looks like.\nimport SwiftUI\n\nstruct ContentView: View {\n    // ⬇️ Q3: Where is the data? \n    // Data is held explicitly inside the struct instance using Property Wrappers like @State.\n    @State private var userName: String = \"Charlotte\" \n\n    // ⬇️ Q2: When does render happen? \n    // ANY time the property marked as @State explicitly changes, the 'body' automatically re-renders.\n    var body: some View {\n        VStack {\n            Text(\"Hello, \\(userName)\")\n            \n            Button(\"Change Name\") {\n                // ⬇️ Q1: Is this single-threaded?\n                // Yes. By default, ALL UI code executes strictly on the Main Thread.\n                userName = \"Swift Developer\"\n            }\n        }\n    }\n}\n\nQ1. Is this single-threaded?\n\nUI rendering in Swift is purely single-threaded.\nAll interface rendering runs on what Apple calls the Main Thread.\nIn the code block above, when you tap the button, that line of code immediately executes on the Main Thread to guarantee a buttery-smooth 60 to 120 frames per second response.\nRule: If you fetch data from the internet (which is slow), you must explicitly push that task to a “Background Thread” to avoid freezing the screen.\n\n\n\nQ2. When does the render happen?\nRendering is reactive, triggered purely by a change in State.\n\nWhen the button is tapped and userName changes from “Charlotte” to “Swift Developer”, SwiftUI detects the change in @State and immediately invalidates the current body.\nSwiftUI then re-computes the physical screen to match the updated state. This is highly optimized and functionally matching how React triggers a re-render when you call setState().\n\n\n\nQ3. Where is the data stored?\nFact: Local state data is stored specifically inside the Struct instance using Property Wrappers.\n\nStructs are highly efficient, temporary data structures.\nThe data (userName) only exists for as long as this specific screen is visually alive on the device.\nIf you have global data (state you want to share across multiple screens), you store it in classes called ObservableObjects, which act very much like React Context or an overarching Redux Store.",
    "crumbs": [
      "<span class='chapter-number'>2</span>  <span class='chapter-title'>Anatomy of a Basic App</span>"
    ]
  },
  {
    "objectID": "chapters/objects.html",
    "href": "chapters/objects.html",
    "title": "Objects & Data Structures",
    "section": "",
    "text": "If numbers are objects, what happens if you build a custom object and try to add them together with +?",
    "crumbs": [
      "<span class='chapter-number'>3</span>  <span class='chapter-title'>Objects & Data Structures</span>"
    ]
  },
  {
    "objectID": "chapters/objects.html#structs-value-types",
    "href": "chapters/objects.html#structs-value-types",
    "title": "Objects & Data Structures",
    "section": "Structs (Value Types)",
    "text": "Structs (Value Types)\nThe struct is the backbone of Swift and the default choice for data modeling (and all SwiftUI views).\n\n\n\n\n\n\nTipJS / React Perspective\n\n\n\nReact requires state to be immutable (never mutating variables directly; always using setState). Swift enforces immutability automatically with structs. When you pass a struct, Swift creates an entirely new, disconnected physical copy in memory.\n\n\nKey Traits:\n\nBehavior: Copied on assignment.\nSafety: Modifying a copy does not alter the original.\nInheritance: None. (Use Protocols instead).\n\nstruct User {\n    var name: String\n    var role: String\n}\n\nvar admin = User(name: \"Alice\", role: \"Admin\")\nvar backupAdmin = admin // Creates a distinct, physical copy in memory\n\nbackupAdmin.name = \"Bob\"\n// admin.name safely remains \"Alice\"",
    "crumbs": [
      "<span class='chapter-number'>3</span>  <span class='chapter-title'>Objects & Data Structures</span>"
    ]
  },
  {
    "objectID": "chapters/objects.html#classes-reference-types",
    "href": "chapters/objects.html#classes-reference-types",
    "title": "Objects & Data Structures",
    "section": "Classes (Reference Types)",
    "text": "Classes (Reference Types)\nClasses are used less frequently in modern Swift than in older object-oriented languages. You use them primarily for data that must be shared across different screens simultaneously (like ViewModels).\n\n\n\n\n\n\nTipJS / React Perspective\n\n\n\nA Swift class behaves exactly like a standard JavaScript Object. If you pass it to a new variable, you are just passing a pointer. Changing one changes them all.\n\n\nKey Traits:\n\nBehavior: Shared reference in memory.\nSafety: Mutable across your entire app (requires careful management).\nInheritance: Can inherit from parent classes.\n\nclass DatabaseConnection {\n    var isConnected: Bool = false\n}\n\nlet db1 = DatabaseConnection()\nlet db2 = db1 // Points to the exact same memory address\n\ndb2.isConnected = true\n// db1.isConnected is now also true",
    "crumbs": [
      "<span class='chapter-number'>3</span>  <span class='chapter-title'>Objects & Data Structures</span>"
    ]
  },
  {
    "objectID": "chapters/objects.html#enums-enumerations",
    "href": "chapters/objects.html#enums-enumerations",
    "title": "Objects & Data Structures",
    "section": "Enums (Enumerations)",
    "text": "Enums (Enumerations)\nEnums in Swift are first-class, memory-safe structures used to represent a group of related, mutually exclusive states.\n\n\n\n\n\n\nTipJS / React Perspective\n\n\n\nIn TypeScript, you enforce state using Union Strings (type Status = \"success\" | \"error\" | \"loading\"). Swift enums are the native, highly-optimized equivalent, but they can also carry variables directly inside them.\n\n\nKey Traits:\n\nBehavior: Value type.\nSuperpower: Can attach specific variables directly to specific cases (“Associated Values”).\nUsage: Replaces boolean flags (isLoading, hasError) and magic strings.\n\nenum LoadingState {\n    case idle\n    case loading\n    case success([String]) // Data is attached safely inside the case\n    case error(Error)\n}\n\nlet currentState = LoadingState.success([\"Item 1\", \"Item 2\"])",
    "crumbs": [
      "<span class='chapter-number'>3</span>  <span class='chapter-title'>Objects & Data Structures</span>"
    ]
  },
  {
    "objectID": "chapters/objects.html#actors-concurrency-safe-reference-types",
    "href": "chapters/objects.html#actors-concurrency-safe-reference-types",
    "title": "Objects & Data Structures",
    "section": "Actors (Concurrency-Safe Reference Types)",
    "text": "Actors (Concurrency-Safe Reference Types)\nActors are a modern Swift feature built exclusively to prevent Data Races (when two parts of your app try to change the same memory at the exact same millisecond).\n\n\n\n\n\n\nTipJS / React Perspective\n\n\n\nJavaScript is single-threaded; the Event Loop handles tasks one by one, so parallel memory collisions rarely happen. Swift is genuinely multi-threaded. Code runs in parallel. Actors are required to safely manage shared data across these parallel threads.\n\n\nKey Traits:\n\nBehavior: Reference type (Shared memory).\nSafety: Automatically isolates its state. It forces outside code to wait in a queue (await) to interact with it.\nUsage: High-performance, concurrent background tasks.\n\nactor SafeCounter {\n    var count = 0\n    \n    func increment() {\n        count += 1\n    }\n}\n\nlet counter = SafeCounter()\n\n// Because the actor is heavily guarded, you must pause and \"await\" your turn to access it:\n// await counter.increment()",
    "crumbs": [
      "<span class='chapter-number'>3</span>  <span class='chapter-title'>Objects & Data Structures</span>"
    ]
  },
  {
    "objectID": "chapters/variables.html",
    "href": "chapters/variables.html",
    "title": "Variables",
    "section": "",
    "text": "a Variable is an object reference\nlet is an immutable reference - its value is assigned once and cannot be changed\nvar is a mutable reference - its value can be changed\n\n\n\n\nlet-var.png\n\n\n\nVariables have a type which is established when the variable is declared and cannot be changed later.\n\ntype annotation is optional in Swift\ntype inference is the process of deducing the type of a variable from its initial value\n\n\nvar name = \"Charlotte\"         // inferred as String\nname = 26                     // Error: Cannot assign value of type 'Int' to type 'String'\n\nvariables start with small letter\ntypes start with uppercase letter\nA variable declared at the top level of a file is a global variable\n\n\nObject Members\nclass fly {\n    let name = \"buzz\"\n    func sayName() {\n        print(name)\n    }\n}\n\nname here is a property of the object\n\na variable declared at the top level of an object declaration\n\nsayName here is a method of the object\n\na function declared at the top level of an object declaration\n\n\nIf declared at the top level of an object declaration - it’s an object member. Members of an object define messages that can be sent to the object.",
    "crumbs": [
      "<span class='chapter-number'>4</span>  <span class='chapter-title'>Variables</span>"
    ]
  },
  {
    "objectID": "chapters/functions.html",
    "href": "chapters/functions.html",
    "title": "Functions",
    "section": "",
    "text": "Swift functions use the func keyword, enforce strict types on both the arguments and the output, and explicitly mandate how you call them.\nKey differences compared to JavaScript:\n\nArguments require explicit types (e.g., name: String).\nA function’s return type is declared with a right-facing arrow -&gt;.\nWhen invoking a function, you must explicitly write out the parameter’s name to make the code highly legible.\n\nfunc name(parameterName: parameterType) -&gt; ReturnType {\n    // Code to execute\n    return value\n}\n\nname() becomes a global function existing in the global namespace of your specific module\nyou can call name() in any file within the same module\n\nprivate func name(parameterName: parameterType) -&gt; ReturnType { ... }\n\nYou cannot call this from another file. It is restricted exclusively to the file (or enclosing scope) it was declared in.\n\npublic func name(parameterName: parameterType) -&gt; ReturnType { ... }\n\nYou can call this from any other file, including files in entirely different modules (like if you are building a Framework that another app imports).\n\n// 1. Standard Swift Function\nfunc greet(name: String) -&gt; String {\n    return \"Hello \\(name)\" // Notice string interpolation uses \\( ) not ${ }\n}\n\nlet message = greet(name: \"Charlotte\") // ⬅️ MUST explicitly write 'name:' \n\n\n// 2. SwiftUI View (Struct returning UI)\nstruct Greet: View {\n    let name: String\n    \n    var body: some View {\n        Text(\"Hello \\(name)\")\n    }\n}\n\n// ⬇️ Called exactly like a function using named parameters!\nGreet(name: \"Charlotte\")\n\n\n\n\n\n\nNoteWhat does this look like in JavaScript & React?\n\n\n\n\n\n// 1. Standard JavaScript Function\nfunction greet(name) {\n    return \"Hello \" + name;\n}\n\ngreet(\"Charlotte\"); // Called without explicitly naming the parameters\n\n\n// 2. React Functional Component (Returns UI)\nexport default function Greet({ name }) {\n    return &lt;h1&gt;Hello {name}&lt;/h1&gt;;\n}\n\n// Called using JSX, where props act similarly to named parameters\n&lt;Greet name=\"Charlotte\" /&gt;\n\n\n\nFunction Parameter Omitting: If you want Swift to behave exactly like JavaScript and let you omit the parameter name when calling a function, you explicitly place an underscore _ before the parameter name in the definition.\nfunc fastGreet(_ name: String) -&gt; String {\n    return \"Hey \\(name)\"\n}\n\n// Now it looks exactly like JavaScript when invoked:\nlet fastMessage = fastGreet(\"Charlotte\")\n\nAll executable code must live inside body of a function\na function declared at the top level of a file is a global function\n\nHow often do functions run?\nWhen do the functions run?\nHot reloading?",
    "crumbs": [
      "<span class='chapter-number'>5</span>  <span class='chapter-title'>Functions</span>"
    ]
  },
  {
    "objectID": "chapters/scope.html",
    "href": "chapters/scope.html",
    "title": "Scope",
    "section": "",
    "text": "Levels of scope:\n\na module is a scope\na function is a scope\ncurly braces are a scope",
    "crumbs": [
      "<span class='chapter-number'>6</span>  <span class='chapter-title'>Scope</span>"
    ]
  },
  {
    "objectID": "chapters/global-namespace.html",
    "href": "chapters/global-namespace.html",
    "title": "Global Namespace",
    "section": "",
    "text": "Namespaces",
    "crumbs": [
      "<span class='chapter-number'>7</span>  <span class='chapter-title'>Global Namespace</span>"
    ]
  },
  {
    "objectID": "chapters/global-namespace.html#namespaces",
    "href": "chapters/global-namespace.html#namespaces",
    "title": "Global Namespace",
    "section": "",
    "text": "a namespace is a named region of code.\ncannot be accessed directly without using the dot operator .\n\nclass Dog {             // Dog is a namespace (because it contains a nested class)\n    class Annie {}      // Annie is a nested type\n}\n   \nDog.Annie",
    "crumbs": [
      "<span class='chapter-number'>7</span>  <span class='chapter-title'>Global Namespace</span>"
    ]
  },
  {
    "objectID": "chapters/global-namespace.html#modules",
    "href": "chapters/global-namespace.html#modules",
    "title": "Global Namespace",
    "section": "Modules",
    "text": "Modules\n\nmodules are actually a namespace too MyApp.Dog.Annie is the true name of class Annie\nimport Foundation / import UiKit are used to import namespaces without the need for dot notation every time",
    "crumbs": [
      "<span class='chapter-number'>7</span>  <span class='chapter-title'>Global Namespace</span>"
    ]
  }
]